modules:
  enable::
    - lmod

  lmod:
    hash_length: 0

    core_compilers::
      - 'gcc@4.8.5'

    blacklist:
      - '%gcc'
      - '%pgi'
      - '%intel'

    whitelist:
      - 'intel-parallel-studio'
      - 'parallel-netcdf'
      - gcc
      - intel
      - libgeotiff
      - libpng
      - lmod
      - netcdf
      - openmpi
      - pgi
